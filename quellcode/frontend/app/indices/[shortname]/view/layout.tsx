export const metadata = {
  title: 'Index View',
  description: 'View index performance and historical data'
};

export default function IndexViewLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return children;
}